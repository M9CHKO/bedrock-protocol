# CPE Relay for Android

Автономное Android-приложение для запуска Bedrock Live Relay без Termux.
Приложение содержит C++-библиотеку, слушает Minecraft на фиксированном
UDP-порту `19132` и подключает его к серверу, который пользователь вводит в
меню. Версия протокола выбирается из фактического списка версий, встроенных в
библиотеку; relay не переводит пакеты между разными версиями.

## Готовый APK

- [CPE Relay 1.2.20 (arm64-v8a, optimized Release, debug-signed)](apk/CPE-Relay-v1.2.20-arm64-v8a-release-debug-signed.apk)
- [SHA-256](apk/CPE-Relay-v1.2.20-arm64-v8a-release-debug-signed.apk.sha256)

Минимальная версия — Android 8.0 (API 26), целевая — Android 16
(API 36). APK содержит только `arm64-v8a`. Сборка подписана тем же debug-
сертификатом, что и предыдущие тестовые APK, поэтому устанавливается поверх
них через обычное обновление.

## Что исправлено в 1.2.20

- В меню автозаполнения добавлена высота 1–5 блоков. Точки теперь задают
  нижний слой на уровне ног игрока, а объём строится снизу вверх, чтобы готовый
  слой мог служить опорой следующему.
- Перед работой область постепенно сверяется с загруженным миром. Любой уже
  стоящий блок считается обработанным и пропускается; известные клетки больше
  не перечитываются на каждом пакете движения.
- Автопроход использует сетевую высоту глаз `feet + 1.62`, габариты игрока
  0.6×1.8 блока и проверяет каждую колонку под AABB. Разрешён безопасный шаг
  вверх/вниз на один блок, а заблокированное прямое направление обходится
  через свободную соседнюю клетку без диагонального срезания углов.
- Пакеты установки блока сверены с веткой dev
  [`weathertop_bot`](https://github.com/boredfedya/weathertop_bot/tree/dev):
  `item_use` сохраняет авторитетный stack ID и точную legacy-структуру, а
  перед действием повторно синхронизируется `MobEquipment`. Пополнение хотбара
  сначала использует подтверждаемый `ItemStackRequest swap`, затем совместимый
  legacy fallback.
- Устранён показанный в журнале ANR `Input dispatching timed out`: применение
  нативных настроек, публикация и очистка сотен маркеров больше не удерживают
  главный Android-поток или его блокировку. Снимок маркеров копируется под
  короткой блокировкой, а сортировка и кодирование выполняются вне неё.
- Сохранено исправление 1.2.19: неизменные контуры имеют постоянные ID и не
  переотправляются в долгой сессии. Подробный журнал по-прежнему ограничен
  двумя сегментами по 256 КиБ и очищает файлы старше суток.

## Что исправлено в 1.2.19

- Авто-тотем и авто-броня теперь первым делом отправляют точный текущий
  `swapBetween` из `weathertop_bot`: исходные `has_stack_id/stack_id`
  сохраняются, пустой предмет имеет полную структуру, а `offhand` и `armor`
  кодируются штатным `WindowIDZigzag32`.
- Перенесены остальные реально существовавшие варианты Weathertop: старые
  числовые ID `238/240` через `WindowIDVarint` с последующим
  `MobEquipment`, а также вариант записанного клиентского пакета без stack ID.
- Подтверждаемые варианты больше не отправляют неверную одно-пакетную цепочку
  через cursor. Для пустого слота используется `take`, для занятого — `swap`;
  источник выбирается как `hotbar` или `inventory`, затем проверяются варианты
  `hotbar_and_inventory` и `inventory`.
- Перед `ItemStackRequest` relay открывает серверный инвентарь игрока, скрывает
  служебный `ContainerOpen` от мобильного клиента и гарантированно закрывает
  окно после ответа. Принятый результат применяется только после
  `ItemStackResponse` либо фактического серверного обновления экипировки.
- Устранено накопление обводок в долгих сессиях. Неизменившиеся 512 рамок
  схемы и 384 рамки автозаполнения больше не переиздаются каждые четыре
  секунды; фигуры имеют постоянные ID и отправляются только как изменения и
  точечные удаления. При неподвижной сцене трафик подсветки равен нулю.
- Текстурные фантомы схемы и их ориентация не изменялись.

## Что добавлено в 1.2.18

- Добавлен модуль автозаполнения горизонтальной области. Две точки задают
  прямоугольник, 3–8 точек — произвольный полигон; точки фиксируются по позиции
  игрока, а вся рассчитанная область и текущий маршрут подсвечиваются нативными
  блоковыми рамками Minecraft.
- Тип строительного блока берётся из основной руки при запуске. После
  опустошения хотбара relay ищет такой же блок во всём синхронизированном
  инвентаре, переносит его в выбранный слот и продолжает только после
  подтверждения сервера.
- Автопроход двигает одновременно серверного игрока и локальную камеру,
  проверяет пол, свободное место для тела и головы, уступает ручному управлению
  и останавливается при отсутствии безопасного пути. Если блоки закончились,
  повторный запуск выполняется только кнопкой пользователя.
- Добавлена отдельная небольшая плавающая кнопка запуска/остановки. Её можно
  перетаскивать по экрану и менять размер в меню автозаполнения.
- Обводки схемы и области больше не копятся бесконечно: одновременно живёт
  ограниченное ближайшее окно, освобождённые ID переиспользуются, старые ID
  удаляются до добавления новых, а конечный TTL очищает даже потерянный пакет.
- Автототем и автоброня теперь перебирают подтверждаемые варианты из
  `weathertop_bot`: прямой stack swap, cursor take/swap/place для двух типов
  инвентарного контейнера, официальный legacy и legacy-кодек weathertop.
  `ItemStackResponse` обрабатывается явно, отказ сервера переключает вариант.
- Сохранены остальные исправления долгих сессий и подключения из 1.2.17.
  Отрисовка текстур схемы не изменялась.

## Что исправлено в 1.2.17

- Подключение Minecraft к локальному relay стало устойчивее: UDP-буфер RakNet
  на Android увеличен под крупный фрагментированный Login со скином. Если такой
  вход всё же не собрался за 20 секунд, зависшая сессия автоматически
  освобождается, в уведомлении и журнале появляется понятная ошибка, а
  следующая попытка больше не ждёт минутный тайм-аут.
- Устранены найденные причины постепенных лагов в долгой сессии. Число
  полностью декодированных колонок мира, ожидающих пакетов и тайлов мини-карты
  теперь жёстко ограничено мобильными значениями; дополнительный кеш сырых
  чанков ограничен 48 МиБ вместо 96 МиБ.
- При реальном сигнале нехватки памяти от Android relay освобождает дальние
  необязательные кеши мира, очередь декодирования и производные таблицы. Простое
  скрытие окна приложения за Minecraft не очищает активный мир.
- Подробный журнал хранит не более двух сегментов по 256 КиБ и автоматически
  удаляет неактивные файлы старше суток. Одинаковые сообщения и чрезмерный
  поток DEBUG сворачиваются в итоговые счётчики, а дорогая синхронная запись на
  накопитель выполняется только при настоящем падении процесса.
- Авто-тотем и авто-броня снова используют уже полученный полный снимок всего
  инвентаря. Одиночное обновление слота больше не выдаётся за полную
  синхронизацию, после таймаута кеш не теряется навсегда, а неподтверждённые
  сервером перемещения повторяются с безопасной увеличивающейся задержкой.
- Состояние экипировки больше не меняется в приложении заранее: предмет
  считается надетым только после подтверждения от сервера. В меню показывается
  найденный слот тотема или брони, что позволяет сразу проверить сканирование.
- Текстуры схем, внешний контур и пользовательские цвета оставлены без
  изменений относительно 1.2.16.

## Что исправлено в 1.2.16

- Удалён полноэкранный слой, который пытался накладывать текстуры схемы через
  положение камеры. Текстурные фантомы снова создаются внутри Minecraft через
  нативный `falling_block`, как в версии 1.2.14, поэтому они больше не плавают
  по экрану и не зависят от камерной проекции Android.
- Остальные изменения сохранены: у блока остаётся одна расширенная внешняя
  рамка без внутреннего контура, расширенная палитра и самостоятельный выбор
  RGB-цвета для каждого состояния схемы.

## Что исправлено в 1.2.15

- Текстуры схемы больше не создаются через `falling_block`: клиент Bedrock
  отображал такую сущность полным кубом даже при runtime ID ступени. Новый
  click-through слой рисует официальный PNG по collision-частям уже
  повёрнутого Bedrock-состояния. Ступени получают две части, направление
  `weirdo_direction` и положение `upside_down_bit`; плиты учитывают верх/низ.
- Для верхней и нижней граней UV поворачивается и отражается вместе со схемой.
  Индекс официального resource pack теперь отдельно сохраняет `north`,
  `south`, `east`, `west`, `top` и `bottom`, поэтому направленные стороны не
  заменяются одной случайной боковой текстурой. Уже импортированный FULL ZIP
  нужно один раз импортировать повторно, чтобы пересобрать расширенный индекс.
- У каждой collision-части оставлена только одна слегка расширенная внешняя
  рамка. Точная внутренняя копия удалена, поэтому двойных линий и внутренних
  швов больше нет.
- Палитра контуров расширена тёмными оттенками. Кнопка **СВОЙ ЦВЕТ** открывает
  RGB-регуляторы и позволяет независимо задать любой цвет правильных,
  неправильных и отсутствующих блоков.

## Что исправлено в 1.2.14

- Для `.schem`, `.schematic`, `.litematic` и Java structure `.nbt` в APK
  добавлена точная таблица состояний Java → Bedrock 1.21.100. Направление,
  верхняя/нижняя половина и ось переводятся до выбора runtime ID, поэтому
  ступени, плиты, люки, двери, брёвна и рельсы получают нужное состояние.
- Поворот и зеркало применяются к Java-состоянию до перевода. Текстурный
  фантом теперь получает уже окончательные Bedrock-свойства
  (`weirdo_direction`, `upside_down_bit`, `minecraft:vertical_half` и другие),
  без повторного поворота или возврата к состоянию по умолчанию.
- Настройка цветной заливки снова стала настройкой текстур. Фантомы остаются
  клиентскими сущностями без коллизии и не записываются в настоящий чанк;
  точный двойной контур по-прежнему показывает collision-форму блока.

## Что исправлено в 1.2.13

- Ожидаемая текстура блока больше не показывается через `falling_block`,
  поскольку Bedrock теряет у такой сущности направление ступеней и других
  состояний. Вместо неё отсутствующая ячейка получает полупрозрачную заливку
  из цветного стекла, ближайшего к выбранному цвету отсутствующего блока.
- Контур каждой collision-части рисуется двумя рамками с небольшим внешним
  разносом. Полные отсутствующие блоки также снова получают рамку, поэтому
  схема остаётся заметной на белом кварце и при ярком дневном освещении.
- Точная форма и направление ступеней, плит и люков берутся из collision-
  геометрии, а цветная заливка служит только хорошо видимой меткой ячейки и
  больше не выдаёт неправильную ориентацию за правильную.

## Что исправлено в 1.2.12

- Восстановлены текстурные фантомы на протоколе 1.21.100. Поле
  `base_runtime_id` отсутствует в словаре метаданных этой версии и полностью
  останавливало публикацию схемы; совместимый `falling_block` снова получает
  runtime ID ожидаемого состояния через поддерживаемое поле `variant`.
- Кнопки X/Y/Z больше не отбрасываются во время асинхронного поиска
  поверхности. Нажатое смещение запоминается и применяется к новому якорю,
  а каждое последующее перемещение явно фиксируется в журнале.
- При отказе нативного рендера повторные попытки получают экспоненциальную
  задержку до 30 секунд, поэтому одна несовместимость больше не создаёт сотни
  одинаковых ошибок в секунду.

## Что исправлено в 1.2.11

- Текстурные фантомы переведены с `falling_block` на клиентские
  `moving_block` с точным `base_runtime_id`. Клиент получает полное Bedrock-
  состояние блока, поэтому ступени, люки и другие направленные модели больше
  не должны застревать в одном направлении.
- В меню схемы появились отдельные переключатели текстур и контуров,
  прозрачность текстур, видимость контуров и независимые цвета для правильно
  поставленного, неправильного и отсутствующего блока.
- Ограничение яркости зелёного контура удалено. Выбранная прозрачность теперь
  применяется одинаково к геометрически точным зелёным, красным и жёлтым
  рамкам, включая части ступеней и плит.
- Закрытое меню CPE запоминает своё состояние. Когда Minecraft закрывает чат,
  инвентарь, сундук, шалкер или другой экран, служебная вкладка возвращается,
  но сама панель больше не раскрывается без нажатия пользователя.

## Что исправлено в 1.2.10

- Зелёные и красные рамки теперь повторяют настоящую collision-геометрию
  ожидаемого состояния блока. Правильно поставленный блок получает слабую
  зелёную подсветку, неправильный — красную; ступени, плиты, люки и другие
  неполные блоки больше не обозначаются одинаковыми маленькими кубиками.
- Для прямых ступеней строятся две ориентированные части по
  `weirdo_direction` и `upside_down_bit`. Поэтому контур однозначно показывает
  сторону подъёма и верхнее/нижнее положение даже там, где клиентский
  `falling_block` не поворачивает модель сущности.
- Исправлен размер `DebugBox`: в 1.21.100 позиция задаётся нижним углом, но
  `box_bound` содержит полный размер формы. Начиная с 1.26.10 автоматически
  используется новая семантика с позицией в центре.
- Каждая часть контура получила стабильный relay-owned ID. При изменении
  одного блока обновляется только его форма и цвет, без удаления и повторной
  отправки всей башни. Количество форм ограничено 3 600; слишком сложные
  модели безопасно объединяются в общий AABB, чтобы не создавать лаги.
- Для отсутствующих полноразмерных блоков остаётся только текстурный фантом,
  без лишней сетки. Слабый контур сохраняется лишь у направленных и неполных
  форм, где он нужен для понимания ориентации.
- Имя блока без списка свойств теперь разрешается в объявленное
  `defaultState`, а не в случайное первое состояние. Отдельным тестом
  проверены четыре направления кварцевых ступеней и их разные runtime ID.

## Что исправлено в 1.2.9

- Текстурные фантомы получили стабильные relay-owned ID и обновляются только
  по изменившимся клеткам. Движение игрока и новая ревизия чанка больше не
  удаляют всю схему перед повторной отрисовкой, поэтому при входе внутрь
  постройки она не должна исчезать на несколько секунд.
- Лимит текстурных подсказок увеличен с 64 до общего безопасного лимита 1 800.
  Для ещё не декодированных клеток также показывается ожидаемая текстура, так
  что схема из 1 290 блоков не получает постоянных пустых участков во время
  загрузки чанков.
- Голубые рамки отсутствующих блоков убраны. Текстура показывает нужный блок,
  а красная рамка остаётся только там, где сервер подтвердил неправильный
  настоящий блок. Правильно установленные блоки не подсвечиваются.
- Добавления и удаления фантомов чередуются порциями до 64 пакетов и 48 КиБ.
  При переносе сначала появляются новые клетки, затем удаляются старые; один
  большой сетевой batch больше не создаёт резкий лаг на Android.
- Перенос схемы кнопками X/Y/Z теперь сразу уведомляет контроллер, отменяет
  устаревший снимок и публикует новый якорь без переподключения. Во время
  короткого пересчёта последняя целая схема остаётся видимой.
- Очередь `LevelChunk`/`SubChunk` увеличена до 256 заданий. Переполнение или
  ошибка одной секции инвалидируют только затронутую колонку/секцию, а не весь
  кэш мира, поэтому мини-карта и проверка схемы не теряют остальные чанки.
- При повороте и отражении схемы теперь преобразуются состояния направленных
  блоков: двери и петли, лестницы и люки, рельсы, таблички, колокола,
  аметистовые почки, лианы, грибы и остальные Bedrock-направления. Учтены
  отдельные кодировки дверей и `small_dripleaf_block`, из-за которых их
  текстуры раньше смотрели не в ту сторону света.

## Что исправлено в 1.2.8

- Рамки схемы для Bedrock 1.21.100 теперь совпадают с сеткой настоящих
  блоков. Учтено поведение этой версии клиента, где позиция `DebugBox`
  трактуется как нижний угол: размер уменьшен с двух блоков до точного
  `1×1×1`, а полублочное смещение удалено.
- До 64 ближайших отсутствующих блоков дополнительно показываются как
  клиентские `minecraft:falling_block` с настоящей текстурой ожидаемого
  состояния из палитры загруженной схемы. Поэтому рядом с игроком видно не
  только место, но и материал, который нужно поставить; остальные клетки
  сохраняют лёгкие голубые рамки, а неправильные блоки — красные.
- Текстурные фантомы используют отдельные relay-owned entity ID, нулевую
  коллизию, отключённые AI и гравитацию. Они не записываются в `LevelChunk`,
  не отправляются upstream-серверу и удаляются при обновлении схемы, смене
  измерения, отключении и переподключении.
- Состояние `.mcstructure` преобразуется через уже загруженный Bedrock block
  registry в обычный или hashed runtime ID согласно активному `StartGame`.
  Неизвестные/нестандартные состояния безопасно остаются рамками.
- Добавлен protocol-827 smoke-тест полного `AddEntity`/`RemoveEntity`:
  кодирование, strict decode, metadata `variant`/`scale`/flags, координаты,
  размеры hitbox и повторное кодирование проверены для 1.21.100.

## Что исправлено в 1.2.7

- Устранён native-crash при публикации большой схемы. Кодировщик больше не
  вкладывает полную копию родительского packet object с массивом `shapes` в
  каждый куб: переходы ProtoDef `../` используют краткоживущую невладеющую
  ссылку. Для схемы из 1 290 блоков расход временной памяти теперь растёт
  линейно, а не квадратично.
- Добавления и ключевые удаления `ServerScriptDebugDrawerPacket` ограничены
  64 фигурами на пакет. Схема из 1 290 блоков передаётся 21 упорядоченным
  пакетом с непрерывными `network_id`, без прежнего потока из 1 291 пакета и
  без одного чрезмерно большого массива.
- Предупреждение `camera_orientation ... std::bad_alloc` оказалось вторичным
  следствием исчерпания native heap во время кодирования схемы. Отдельной
  ошибки координат камеры не обнаружено; после устранения аллокационной бури
  этот fallback больше не должен срабатывать.
- Regression-тест кодирует и строго декодирует все 1 290 фигур, затем проверяет
  21 пакет добавления и 21 пакет удаления, границу 64 фигур, первый и последний
  глобальные ID. Отдельный тест сохраняет работу `compareTo="../action"`.

## Что исправлено в 1.2.6

- Схема для Bedrock 1.21.100 переведена со старого пакета
  `ClientboundDebugRendererPacket`, который retail-клиент принимал, но не
  рисовал, на соответствующий протоколу 827 `ServerScriptDebugDrawerPacket`.
  Голубые кубы показывают отсутствующие блоки, красные — неправильные; это
  только клиентские wireframe-фигуры без коллизии и изменения настоящего мира.
- Все видимые кубы теперь группируются в один пакет вместо отдельного пакета
  на каждый блок. Старые фигуры удаляются ключевыми записями из отдельного
  пространства ID, поэтому уменьшение области, смена измерения, отключение и
  переподключение не оставляют хвостов и не затрагивают debug-фигуры сервера.
- Быстрые нажатия кнопок смещения якоря объединяются в одно итоговое обновление
  через 350 мс. Перемещение схемы больше не повторяет применение всех runtime-
  настроек и не создаёт сотни полных перестроений в журнале.
- Авто-тотем и авто-броня для IGN используют совместимый legacy
  `InventoryTransaction` с парой подтверждённых сервером действий
  `inventory ↔ offhand/armor`, как рабочий путь `weathertop_bot`, вместо
  неподдерживаемого сервером `ItemStackRequest`.
- Smoke-тест протокола фиксирует точные байты добавления и удаления
  `ServerScriptDebugDrawerPacket`, включая varuint64 ID, optional-поля,
  координаты центра блока и BEARGB-цвет.

## Что исправлено в 1.2.5

- Декодер принимает modern-v8/v9 секции чанка с нулевым количеством storage.
  IGN/Nukkit использует эту допустимую форму для полностью воздушной секции;
  раньше она давала `expected storage count ... got 0` и оставляла мини-карту
  без данных.
- Пустая секция теперь сохраняет свой Y, считается известным воздухом и не
  сдвигает чтение следующей заполненной секции. Резервный разбор также
  принимает двухбайтовую финальную v8-секцию и по-прежнему не изменяет
  настоящий `LevelChunk`, пересылаемый Minecraft.
- Последовательные IGN/Nukkit v8-секции снова размещаются от Y=0, как в
  `weathertop_bot`/`prismarine-chunk`; только v9 использует подписанный Y из
  самого пакета. Это убирает ошибочный вертикальный сдвиг мира на 64 блока.
- Для пустых секций используется настоящий runtime ID `minecraft:air` из
  активного реестра `StartGame`, включая серверы без хешированных block ID.
  Поэтому первый `UpdateBlock` не превращает остальные воздушные клетки слоя
  в ложные блоки мини-карты или красные несовпадения схемы.
- Благодаря восстановленному кэшу мира схема больше не оставляет все блоки в
  состоянии `unknown`: отсутствующие блоки получают голубые клиентские
  маркеры, неправильные — красные, а подтверждённые сервером правильные блоки
  не подсвечиваются.

## Что исправлено в 1.2.4

- Экранная «вторая камера» схемы удалена. Схема рисуется только подключённому
  через relay клиенту штатными `ClientboundDebugRendererPacket`/кубами:
  голубой означает отсутствующий блок, красный — неправильный, а подтверждённый
  сервером правильный блок больше не подсвечивается. Кубы не имеют коллизии,
  не подменяют настоящий мир и не отправляются удалённому серверу.
- Настоящие `LevelChunk` и `SubChunk` передаются Minecraft без изменения.
  Отдельный кэш сравнения обновляется только подтверждёнными сервером
  `UpdateBlock`, `UpdateBlockSynced` и первичным слоем
  `UpdateSubChunkBlocks`; обычные строительные пакеты клиента проходят upstream
  побайтно без переписывания.
- Маркеры пересобираются по событиям мира, а не каждый кадр. Одинаковые наборы
  не дублируются, показываются только ближайшие загруженные блоки, обновление
  мини-карты вынесено из сетевого callback, а журнал состояния содержит
  счётчики всего/правильно/отсутствует/неправильно/неизвестно/показано.
- При смене измерения, переподключении, выгрузке чанка, переносе или выключении
  схемы клиентские маркеры очищаются. Проверка revision/dimension не позволяет
  фоновой работе вернуть фантомы от прежнего мира или старого якоря.
- Декодер чанков совместим с ответами IGN/Nukkit: последовательные modern-v8
  секции начинаются с Y=-64, поддержан `AllCopied` height-map type, а корректные
  block-sections сохраняются даже при нестандартном хвосте `LevelChunk`.
  `SubChunk` и последующие block updates теперь также обновляют мини-карту,
  вместо накопления повторяющихся ошибок декодирования.

## Что исправлено в 1.2.3

- Схема теперь использует один целочисленный мировой transform для отрисовки,
  проверки блоков, поворота и отражения. Убраны зависимая от скорости FOV и
  дополнительная задержка кадра, из-за которых проекция дёргалась и смещалась
  на несколько пикселей при малых движениях камеры.
- Якорь по Y ставится в первую целую клетку над фактической верхней гранью
  collision shape. Это устраняет дробное смещение схемы относительно блока.
- Relay сверяет строительные блоки схемы с `LevelChunk`, `UpdateBlock` и
  пакетными `UpdateSubChunkBlocks`: правильно поставленный блок полностью
  исчезает вместе с текстурой, неправильный показывается красным. По мере
  строительства открывается следующий внутренний слой схемы.
- Relay больше не увеличивает клиентский publisher radius при удержании чанков:
  серверный пакет идёт в Minecraft побайтно без изменений. Кэширование чанков
  остаётся внутренней функцией relay.
- 15-секундный login-watchdog больше не отправляет Minecraft пакет
  `disconnect`. Медленный вход только фиксируется в диагностике, а новая
  попытка подключения по-прежнему может заменить зависшую сессию.
- Локальный и удалённый RakNet timeout увеличен до 120 секунд; журнал теперь
  сохраняет исходный сигнал `remote_disconnect_notification`,
  `connection_lost_or_timeout` или `incompatible_raknet_protocol` перед
  удалением сессии.
- Исключения в фоновых RakNet- и сетевых очередях теперь завершают только
  проблемную сессию с точной причиной в журнале, а не весь процесс relay.

## Что добавлено в 1.2.1

- Серверные resource pack теперь проходят напрямую между настоящим сервером и
  Minecraft. Клиент получает штатное окно загрузки обязательного пака, сам
  запрашивает его части и подтверждает завершение. Relay сохраняет исходные
  байты этих транспортных пакетов, включая неизвестные добавочные поля, и не
  пишет в журнал `content_key` или секретные параметры CDN.
- Пакетный HUD разделён на независимые страницы: обводка, чанки, снаряжение,
  мини-карта, автоматизация, анализ угроз, схемы и журнал. Интерактивны только
  сами окна и вкладка CPE; прозрачная область поверх Minecraft не должна
  перехватывать нажатия.
- Обводки игроков, мобов и выпавших предметов включаются отдельно. Для каждой
  категории выбирается цвет; общими настройками задаются толщина, дальность и
  FOV. Камера и движение сущностей прогнозируются только по Bedrock-пакетам.
- HUD снаряжения показывает правую и левую руки, четыре слота брони, остаток
  прочности и зачарование. Расчёт износа использует NBT `Damage` и
  version-aware реестр предметов.
- Мини-карта строится из полученных `LevelChunk`, меняет радиус, размер и
  круглую/квадратную форму и перетаскивается по экрану. Счётчик удержанных
  чанков также можно перемещать, масштабировать и сворачивать.
- Анализ угроз отличает опасных враждебных мобов, учитывает расстояние,
  сближение, здоровье, броню, эффекты и приблизительный входящий урон. Порог,
  цвет и размер перемещаемого предупреждения настраиваются пользователем.
- Авто-тотем и авто-броня используют штатные inventory/equipment-пакеты.
  Обе функции выключены по умолчанию и включаются независимо.
- Добавлен отдельный модуль схем с импортом `.mcstructure`, vanilla structure
  `.nbt`, `.litematic`, Sponge `.schem` и legacy `.schematic`. Поддерживаются
  обычный и GZip-сжатый NBT, библиотека импортированных схем, выбор слоя,
  прозрачность, дальность, поворот на 90°, отражение и мировой якорь.
- Папке со схемами можно один раз выдать постоянный Android SAF-доступ. После
  этого совместимые файлы выбираются прямо в меню поверх Minecraft без выхода
  с сервера; запасной системный выбор одного файла остаётся доступен.
- Официальный `FULL` ZIP из Mojang `bedrock-samples` теперь даёт не только
  item-текстуры для HUD, но и block-текстуры для схем. Импортёр читает
  `blocks.json` и `terrain_texture.json`, извлекает PNG, преобразует нужные TGA
  и сопоставляет верхнюю, боковую и нижнюю грани блоков.

Серверный resource pack и локальный архив `bedrock-samples` — разные вещи.
Первый Minecraft скачивает в рамках подключения к серверу. Второй пользователь
необязательно импортирует в CPE Relay только для оформления HUD и 3D-схем; он
не отправляется серверу и не меняет ресурсы самого Minecraft.

Версия 1.1.1 исправляет проекцию рамок при наклоне камеры и на краях экрана.
Почти вертикальный взгляд больше не подменяется направлением тела игрока;
пакетная вертикальная FOV плавно адаптируется к движению, а на средней скорости
используется мягкий профиль без снижения частоты отрисовки. Пропущенное при
входе событие больше не оставляет игрока без меню: актуальный snapshot сессии
восстанавливает overlay и повторяет его создание с ограниченной частотой.

Расчёт прочности теперь следует проверенной схеме из
[`weathertop_bot` (`dev`, commit `6b983ce`)](https://github.com/boredfedya/weathertop_bot/blob/6b983ceaf901bbfdd65d035334db7872a5ba756e/plugins/inventoryManager.js):
текущий износ читается только из NBT `Damage`/`damage`, а максимальная прочность
берётся из реестра предметов именно выбранной версии. Поддержаны обе формы item
NBT (до и после `1.16.220`) и все версии, доступные в селекторе приложения.
Локальная реализация сверяется с профилями встроенного `minecraft-data`:
учитывает старую
прочность арбалета, появление кисти, волчьей брони и булавы, а при неполных
таблицах использует проверенный профиль `1.21.100`. Поле варианта `metadata`
больше не может ошибочно стать износом. Native-код передаёт HUD уже готовые
максимум, остаток и округлённый процент.

Также версия 1.1.1 добавляет одноразовый импорт официального `FULL`-архива
Mojang [`bedrock-samples`](https://github.com/Mojang/bedrock-samples/releases).
На главном экране и во вкладке «Снаряжение» игрового
меню есть кнопка импорта и ссылка на официальные релизы. Импортёр находит
`resource_pack/textures/items/*.png` даже внутри папки версии архива, проверяет
пути, PNG-заголовки, размеры и общий лимит, а затем атомарно сохраняет только
нужные item-текстуры во внутренней памяти Android. Полный архив не
распаковывается, файлы Mojang не встраиваются в APK и не публикуются в Git.

HUD сопоставляет registry-name предмета с настоящим PNG, включая варианты
Bedrock вроде `golden_helmet → gold_helmet`, `wooden_sword → wood_sword` и
анимированные состояния лука, арбалета и удочки. Если точного PNG нет,
остаётся встроенный силуэт. Официальная текстура
`enchanted_item_glint.png`, когда она присутствует в архиве, накладывается на
зачарованную вещь с плавным движением. Работающий overlay замечает замену
набора и перечитывает изображения без перезапуска relay.

Версия 1.1.0 заменяет резкое продвижение рамки при самом первом малом
движении камеры накоплением согласованных микроповоротов и непрерывным
адаптивным экранным фильтром. Рамка больше не сбрасывает свою координату на
каждом активном кадре камеры; центр и размер проходят согласованную
интерполяцию, а неполные, чрезмерно крупные и слишком далёкие проекции не
рисуются. Игроки, мобы и выпавшие предметы получили независимые переключатели,
цвета, предел дальности и регулируемую толщину рамки.

Главный экран и меню поверх Minecraft разделены на аккуратные вкладки.
Добавлены перемещаемый, сворачиваемый и масштабируемый счётчик удержанных
чанков, а также компактный правый HUD руки и четырёх слотов брони с остатком
прочности и анимированной отметкой зачарования. Силуэты HUD адаптированы из
Game-icons.net (Lorc и Delapouite, CC BY 3.0); полная атрибуция включена в
ресурсы APK. Журнал вынесен в отдельную вкладку: при выключенной записи Java-
файл, native-очередь и flight-recorder не получают новых строк. Все HUD-данные
по-прежнему берутся только из Bedrock-пакетов — захват экрана и внедрение в
Minecraft не используются.

Версия 1.0.21 делает пакетное предсказание камеры устойчивым к неравномерной
доставке снимков между native-кодом и Android. Теперь вместе с позицией и
`camera_orientation` атомарно передаётся клиентский `PlayerAuthInput.tick`;
скорость вычисляется по стабильной шкале Bedrock 20 Гц, а не по колеблющемуся
времени прихода. Один выброс скорости ограничивается медианой трёх последних
наклонов, при остановке скорость обнуляется сразу, а при развороте старое и
новое направления не смешиваются. Длина дополнительного прогноза зависит от
доверия к траектории и ограничена 85 мс; мировой предиктор мобов использует ту
же безопасную оценку скорости. Добавлены тесты джиттера доставки, выброса,
остановки, разворота и проекции движения 20 Гц при отрисовке 120 FPS. Захват
экрана и доступ к процессу Minecraft не используются.

Версия 1.0.20 убирает второй независимый экранный фильтр во время движения
камеры. Положение всех рамок теперь сразу вычисляется одной общей проекцией
камеры, а не догоняет её отдельно для каждого моба. Сглаживание позиции
сущности остаётся в мировых координатах; когда камера неподвижна, экранный
фильтр продолжает подавлять мелкий сетевой шум. Переход после остановки камеры
начинается с уже совпавшей координаты, поэтому скрытого догоняющего движения
нет. Автотест проверяет неподвижную сущность при нелинейном повороте на 56° со
120 кадрами/с и отсутствие остаточного смещения после остановки. Реализация
по-прежнему использует только Bedrock-пакеты: запись и захват экрана не нужны.

Версия 1.0.19 убирает регрессию 1.0.18, измеренную по двум пользовательским
видеозаписям: медианная ошибка рамок выросла примерно вдвое, а выбросы при
повороте камеры — более чем в 2,4 раза. Инерционный экранный `JerkAxis`
полностью удалён. Новый адаптивный фильтр только интерполирует рамку к текущей
проекции, поэтому не может продолжать старую траекторию или перелетать
остановившуюся цель. Координаты центра используют общий коэффициент, как и
ширина с высотой: рамка движется как цельный прямоугольник. Также возвращён
быстрый отклик скорости камеры из 1.0.17, а тесты теперь отдельно проверяют
резкий поворот, сетевые поправки 20 Гц при 120 FPS и разворот без повторного
движения туда-сюда.

Версия 1.0.18 исправляет общий рывок рамок, подтверждённый покадровым
разбором записи с частотой около 108 кадров/с. У экранного фильтра больше нет
жёсткой мёртвой зоны, после которой рамка скачком догоняла новую проекцию.
Центр и размер теперь сохраняют непрерывные положение, скорость и ускорение:
поправка очередного сетевого кадра ограничивает рывок ускорения, но не меняет
экранную координату мгновенно. Позиция игрока и отдельный вектор направления
камеры из одного `player_auth_input` публикуются атомарно, поэтому Android-слой
не может увидеть промежуточную смесь новой позиции со старым направлением.

Версия 1.0.17 исправляет остаточное движение рамок вперёд-назад. Старый
прогноз больше не возвращается к уже устаревшей координате, а монотонно
замедляется до ограниченной точки. После мировой проекции центр, ширина и
высота каждой рамки дополнительно стабилизируются на каждом экранном кадре:
мелкий сетевой джиттер отсекается, перелёт цели запрещён, а при быстром
повороте фильтр автоматически ускоряется. Overlay также явно использует
аппаратно-ускоренный Canvas.

Версия 1.0.16 убирает рывки рамок при повороте камеры и движении сущностей.
Положение, направление и скорость теперь проходят непрерывный критически
затухающий фильтр, который не перезапускается на каждом сетевом пакете.
Камера опрашивается облегчённым native-снимком каждые 12 мс, а прогноз между
пакетами плавно затухает вместо резкой остановки. Подписи расстояния также
кэшируются, чтобы не создавать лишние объекты на каждом кадре Android.

Версия 1.0.15 исправляет привязку 2D-рамок при движении камеры. Проекция
теперь использует фактический вектор `camera_orientation` из
`player_auth_input`, а не только поворот тела игрока; запаздывающая
интерполяция камеры заменена коротким предсказанием между сетевыми кадрами.
FOV применяется по вертикальной оси с учётом соотношения сторон экрана, поэтому
рамки не должны оставаться на прежнем месте экрана при повороте взгляда.

Версия 1.0.14 добавляет плавные 2D-рамки игроков и мобов поверх Minecraft.
Native-трекер читает позиции из штатных Bedrock-пакетов `add_player`,
`add_entity`, `move_player`, `move_entity`, `move_entity_delta` и удаляет
запись при соответствующем remove/disconnect. Позиция и направление камеры
обновляются из исходящего `player_auth_input`; прозрачный Android-слой получает
снимки каждые 50 мс и интерполирует движение рамок. В drawer есть выключатель
и калибровка FOV проекции от 50 до 110 градусов. Слой существует
только во время готовой игровой сессии и удаляется сразу после выхода.

Это экранная проекция, а не модификация рендера Minecraft: без доступа к его
depth buffer рамки видны сквозь туман и стены, а границы модели остаются
приблизительными. Точнее всего совпадение в виде от первого лица после
настройки FOV; настоящий контур пикселей модели потребовал бы отдельного
модифицированного клиента Minecraft и ломал бы обычное обновление APK.

Версия 1.0.13 добавляет в плавающее меню живую проверку удержания чанков.
Она показывает фактически переданный Minecraft радиус (радиус сервера и
увеличенное relay значение), количество полученных команд publisher,
число и объём точных `level_chunk` в памяти, удаления за радиусом и
срабатывания защитного лимита. Это позволяет проверить функцию независимо от
тумана и собственной дальности отрисовки мобильного Minecraft: туман может
скрывать чанк, который всё ещё находится в памяти. Диагностический журнал
теперь также фиксирует первые команды `network_chunk_publisher_update`, даже
если сервер уже прислал достаточно большой радиус и переписывать пакет не
потребовалось.

Версия 1.0.12 добавляет настоящий сессионный кэш `level_chunk`. Relay хранит
точные финальные байты каждого уже отправленного клиенту чанка по ключу
`(dimension, x, z)`; повторная загрузка той же координаты атомарно заменяет
старое значение. Изменённый `network_chunk_publisher_update` одновременно
просит Minecraft не выгружать такие чанки, пока они находятся в выбранном в
drawer радиусе. При перемещении записи за границей радиуса удаляются, а при
смене измерения, отключении функции или выходе с сервера весь сессионный кэш
освобождается. Для защиты Android-процесса действует дополнительный предел
256 МиБ: при его достижении первыми удаляются самые дальние и старые чанки.
Функция сохраняет только уже полученные данные и не заставляет сервер
присылать новые чанки дальше установленной им дальности.

Версия 1.0.11 исправляет зависшую локальную сессию после выхода из сервера.
Если первая следующая отправка обнаруживает, что RakNet уже перешёл в
`not_connected`, relay теперь считает это обычным закрытием клиента, сразу и
ровно один раз удаляет native- и Bedrock-состояние и уведомляет соответствующий
upstream. Старая очередь больше не создаёт поток `RakNet batch send rejected`.
Состояние плавающего меню дополнительно привязано к событию
`upstream_ready`: после `disconnect` или ошибки устаревший snapshot уже не
может снова открыть drawer, поэтому панель и вкладка исчезают при выходе из
Minecraft-сервера.

Версия 1.0.10 исправляет оставшийся длительный `Checksum mismatch` на
входящем потоке Minecraft. Проверка encrypted batch теперь выполняется на
копии AES-контекста и меняет рабочее состояние только после правильной
keyed-checksum. Если один batch потерян, повреждён или приходит устаревшим,
relay может безопасно найти следующую подтверждённую позицию AES-GCM и
продолжить сессию; отдельный тест воспроизводит наблюдавшийся 104-байтовый
пакет и проверяет следующий пакет после восстановления. Защита не отключена:
неподтверждённые данные не передаются, а после 8 batch / 64 КиБ без успешной
проверки соединение по-прежнему закрывается. В версиях до 1.1.0 события
`encryption_recovery` оставались в основном журнале при отключённых подробных
логах; теперь общий переключатель отключает запись полностью.

Версия 1.0.9 исправляет второй вариант `Checksum mismatch`: при большой
серии разделённых map-пакетов повтор входящего encrypted batch мог прийти уже
после 32 более новых batch и выпадал из короткой replay-защиты 1.0.7. Relay
теперь хранит ограниченную точную историю до 4096 batch / 8 МиБ, сначала
сравнивает fingerprint, а затем сами байты; отложенный повтор не меняет
AES-GCM state и счётчик. Регрессионный тест принимает 128 последовательных
batch и после них повторяет самый первый.

Также исправлен повторный вход, который мог зависнуть сразу после
`request_network_settings`: переиспользованный Minecraft RakNet-транспорт
иногда продолжал нумерацию дейтаграмм далеко от начала новой сессии, поэтому
relay не подтверждал фрагменты большого `login`, а клиент пересылал около
32 КиБ до срабатывания watchdog. Теперь допускается ровно одна ранняя
ресинхронизация только для непрерывного крупного burst; последующие скачки
по-прежнему отклоняются. Отдельный smoke-тест собирает 32-КиБ split-пакет
после такого скачка и проверяет, что второй скачок не принимается.

Плавающее меню теперь оформлено как левый выезжающий drawer. После входа оно
плавно появляется из-за края экрана, по нажатию на прикреплённую вкладку `CPE`
полностью прячется обратно, а сама вкладка перетаскивается вверх и вниз. При
разрыве игровой сессии drawer вместе со вкладкой удаляется.

Версия 1.0.8 добавляет плавающее меню поверх Minecraft. Оно появляется после
успешного входа на сервер, сворачивается в кнопку `CPE` и полностью закрывается
при разрыве игровой сессии. В меню можно в реальном времени отключить подробные
DEBUG/flight-логи (ошибки и основные события сохраняются) и включить удержание
уже загруженных чанков с радиусом от 10 до 64 чанков. Удержание меняет только
радиус `network_chunk_publisher_update`: сервер продолжает присылать новые
чанки в пределах собственного лимита, а клиент дольше показывает уже
полученные. Для меню требуется одноразовое системное разрешение «поверх других
приложений».

Версия 1.0.7 защищает входящий зашифрованный поток от точной повторной
доставки уже принятого RakNet batch: повтор отбрасывается до изменения
AES-GCM state и счётчика, поэтому он больше не приводит к `Checksum mismatch`
и разрыву сессии. Первая настоящая ошибка checksum теперь записывается один
раз с номером `receive_counter`, а пакеты, уже стоящие в очереди на момент
закрытия, не создают каскад одинаковых ошибок. Регрессионный тест одновременно
проверяет 4096 входящих пакетов (включая наблюдавшийся размер 104 байта) и
крупные разделённые исходящие пакеты.

Версия 1.0.6 исправляет разрыв при смене слота хотбара. Если внешний TSV-
каталог протокола недоступен внутри APK, relay теперь выбирает встроенную схему
строго для указанной версии (`1.21.100`), а не схему самой новой встроенной
версии. Поэтому корректный короткий `mob_equipment` с пустым предметом больше
не ошибочно разбирается как новый `ItemV4` и не закрывает локальную сессию.
Flight-recorder семплирует массовые успешные equipment-пакеты на всех стадиях
и при повторной причине выгружает только новые записи, сохраняя ошибки без
семплирования и не переполняя очередь подробного журнала.

Версия 1.0.5 исправляет доказанный разрыв локального login сразу после
`request_network_settings`: первый `network_settings` теперь адресуется
стабильному RakNet GUID, немедленно будит сетевой цикл Android и считается
отправленным только после ненулевого transport receipt. Журнал больше не
показывает ложный `send_batch` при отвергнутой отправке. При login timeout он
дополнительно выводит connection state, send/resend queues и фактические
счётчики отправленных/полученных UDP-байтов.

Версия 1.0.4 добавляет native flight-recorder последних 768 транспортных
событий. Для каждого пакета фиксируются безопасные размер и hash на этапах
`seen`, `forwarded`, `send_packet` и зашифрованного `send_batch`; содержимое
NBT, JWT и токены не записываются. При transport/parse error, разрыве или
15-секундном зависании локального login кольцо автоматически выгружается в
подробный журнал. Исключение фоновой outbound-очереди больше не теряется:
журнал получает точный текст, а повреждённая полусессия закрывается, чтобы не
блокировать повторный вход. После перезапуска Android 11+ журнал также
показывает системную причину предыдущего завершения процесса (`native_crash`,
ANR, signal, low memory и другие); на Android 8+ сохраняется признак внезапного
завершения активного relay.

Сохранена оптимизация 1.0.3: строгая проверка component-rich `item_registry`
обходит NBT без создания полного дерева объектов и без копирования каждого
имени item.
Распространяемая APK компилирует C++ как `RelWithDebInfo` с `-O2` и
compile-time guard от случайной сборки без оптимизации. Это не меняет байты,
порядок или strict validation пакетов. Успешные массовые equipment-события в
подробном журнале семплируются, а ошибки и итоговый счётчик сохраняются
полностью. В `relay_start` видны `nativeBuild=release` и
`compilerOptimized=true`.

Распространяемый APK собирает native C++ с Release-оптимизацией, но остаётся
debug-signed для прямой установки и обновления тестовых сборок.

## Использование

1. Установить APK и открыть `CPE Relay`.
2. Ввести адрес и UDP-порт сервера назначения. По умолчанию это
   `cpe.ign.gg:19132`.
3. Выбрать точную версию Minecraft клиента и сервера, например `1.21.100`.
4. Нажать **Запустить relay и Minecraft**.
5. При первом запуске разрешить CPE Relay показывать меню поверх других
   приложений.
6. Minecraft подключается к локальному `127.0.0.1:19132`; relay также
   отвечает на LAN RakNet ping как `CPE Relay Android`.
7. При первом входе код Xbox появится в приложении и уведомлении. Кнопка
   копирует код и открывает страницу Microsoft для авторизации.
8. Если сервер требует resource pack, подтвердить штатную загрузку в
   Minecraft. Загрузка и ответы клиента проходят через relay автоматически.

Relay работает foreground-service, поэтому продолжает работу, когда поверх
него открыт Minecraft. Остановить его можно в приложении или через действие
в уведомлении.

Повторный локальный вход заменяет застрявшую предыдущую сессию: relay сначала
отправляет предыдущему upstream протокольный Bedrock `disconnect`, закрывает
его RakNet-транспорт и только затем запускает новую серверную сессию.

## Игровое меню и HUD

После готовности upstream слева появляется вкладка `CPE`. Она открывает
страницы функций, перетаскивается только по вертикали и не создаёт полноэкранной
сенсорной области. Отдельные виджеты — мини-карта, предупреждение об угрозе,
чанки и снаряжение — перетаскиваются за собственную панель и запоминают
позицию. Их размер меняется в соответствующей странице меню.

HUD автоматически скрывается при открытии инвентаря, сундука, шалкера, другого
контейнера или чата и возвращается после закрытия интерфейса. Данные берутся из
`player_auth_input`, entity, inventory/equipment, attributes/effects,
`LevelChunk` и связанных Bedrock-пакетов. Захват экрана, Accessibility API,
внедрение кода и чтение памяти процесса Minecraft не используются.

Из-за этого обводка и 3D-схема являются математической проекцией поверх игры,
а не частью рендера Minecraft. У overlay нет depth buffer: рамка может быть
видна через стену или туман, а точность зависит от выбранного FOV, частоты
пакетов и поведения конкретного сервера. Relay не переводит протоколы — версия
Minecraft и сервера должна совпадать с версией в приложении.

## Импорт и отображение схем

1. Открыть `CPE → СХЕМЫ` и один раз выбрать папку со схемами. Android сохранит
   разрешение на эту папку между запусками.
2. Нажать **ИМПОРТИРОВАТЬ В МИР** у нужного файла. Импорт выполняется в фоне,
   а нормализованная копия сохраняется в приватной библиотеке приложения.
3. Включить **Показывать 3D-схему в мире** и нажать
   **ПОСТАВИТЬ ПЕРЕД ИГРОКОМ**.
4. При необходимости изменить якорь по X/Y/Z, поворот, отражение, слой,
   прозрачность и максимальную дальность.

`.nbt` здесь означает NBT-файл структуры с палитрой и блоками, а не любой
произвольный `level.dat` или дамп мира. Импортёр ограничивает размеры и объём
входных данных, чтобы повреждённая или чрезмерно большая схема не заняла всю
память телефона. Для крупных построек renderer сокращает число одновременно
рисуемых collision-частей, сохраняя общий контур.

## Официальные текстуры Mojang

Текстуры не встроены в APK. Чтобы использовать настоящие item- и block-
изображения, скачать официальный `FULL` ZIP на странице
[`Mojang/bedrock-samples`](https://github.com/Mojang/bedrock-samples/releases),
затем выбрать **Импортировать официальный FULL ZIP** на главном экране или во
вкладке **Снаряжение**.

Приложение извлекает только необходимые изображения и таблицы сопоставления во
внутреннее хранилище. После успешного импорта снаряжение и блоки схем получают
официальные текстуры сразу и при следующих запусках relay. Для отсутствующих
вариантов остаются встроенные диагностические цвета или силуэты. Сам ZIP,
полный resource pack и файлы Mojang в APK/Git не копируются.

## Подробный журнал

Отдельная вкладка **Журнал** отображает последние события, а кнопка
**Копировать журнал** копирует полный журнал для отчёта об ошибке. При
выключенном переключателе журнал не получает даже ошибки и аварийные записи.
Когда запись включена, сохраняются
запуск/остановка, локальный и серверный RakNet ping, стадии входа, замена
сессии, strict parse-error и безопасные breadcrumbs item/inventory-пакетов с
размером и хешем без payload. Это позволяет увидеть последний пакет перед
сбоем взаимодействия с предметом и отличить «пакет принят» от «пакет реально
поставлен в RakNet batch». Строки `flight_dump`, `flight.*`, `transport_error`,
`local_login_timeout`, `memory` и `process_exit` нужно прикладывать целиком.
Журнал автоматически удаляет неактивные файлы старше суток, сворачивает
одинаковые строки и хранит не более двух файлов по 256 КиБ во внутреннем
хранилище приложения.
Xbox-коды, тела и заголовки auth-запросов, токены, `content_key`, JWT и
секретные URL-параметры не записываются.

Ответ сервера на RakNet ping показывает его объявленную версию и задержку.
Если она не совпадает с выбранной версией, приложение показывает предупреждение
до подключения Minecraft. В журнале также видны времена downstream handshake,
Xbox HTTPS-запросов и готовности upstream, поэтому долгий вход можно привязать
к конкретной стадии.

## Безопасность и хранение

- Xbox/Bedrock HTTP-запросы выполняются Android `HttpURLConnection` только
  по HTTPS; внешние процессы, `curl` и Termux не используются.
- Токены хранятся во внутреннем каталоге приложения, недоступном другим
  приложениям без root.
- Android backup отключён, чтобы кэш авторизации не попадал в резервную
  копию.
- Тела auth-запросов, `content_key`, токены и CDN-секреты не логируются.

## Сборка

Требуются JDK 17+, Android SDK Platform 36, совместимые Build Tools, NDK
`27.2.12479018` и CMake `3.22.1`.

```text
cd android/relay-app
./gradlew test lint assembleRelease
```

На Windows используется `gradlew.bat`. Итоговый файл создаётся по адресу
`app/build/outputs/apk/release/app-release.apk`.

Установка через ADB:

```text
adb install -r app/build/outputs/apk/release/app-release.apk
```

Gradle/Prefab поставляет OpenSSL для Android, а NDK — zlib и системные UDP
API. JNI-слой встраивает `bedrock_protocol` непосредственно в APK и передаёт
Android HTTPS-транспорт в native Xbox token managers через публичную
`XboxTokenHttpClientFactory`.
